#ifndef __COMPLEX__
#define __COMPLEX__

class Complex {
    private:
        double re, im;
    public:
        inline Complex& Complex::operator += (const Complex& r);
        inline Complex& __doapl(Complex& ths, const Complex& r)
}

#endif